extends layout
block content
  h1= title
  p Welcome to the #{title} server.
  button(type="button",class="btn btn-secondary",onclick="goFaster()") Press Me  
  br 
  br
  #faster Increase The Speed
  br
  a(href="/product") View Product 
  script.
    var mph = 20;
    var change = 1;
    function goFaster(){
    mph = mph + change;
    change = change + 1;
    document.getElementById('faster').textContent = `MPH ${mph} with change ${change}`
    }